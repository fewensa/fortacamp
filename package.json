{
  "name": "fortacamp",
  "version": "0.1.0",
  "description": "fortacamp",
  "private": true,
  "scripts": {
    "build:all": "lerna run build",
    "test:all": "lerna run test",
    "lint:all": "lerna run lint",
    "publish:npm": "lerna publish from-package --loglevel=silly --no-private --no-verify-access --yes -- --verbose"
  },
  "workspaces": [
    "bots/*"
  ],
  "devDependencies": {
    "lerna": "^8.1.9"
  }
}